# english-to-tamil-api

* the tk.py contains englishtotamilapi() function that takes string('english word') as an argument and returns tamil word 
* this project requires interet
*how to start
  - pip install -r requirements.txt
  - python tk.py
happy coding
